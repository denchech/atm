{% extends 'base.html.twig' %}

{% block title %}Log in{% endblock %}

{% block navigation %}
    <a class="nav-item nav-link" href="{{ path('employee_cash') }}">Add cash</a>
{% endblock %}

{% block body %}
    {% if attempts != 0 %}
        <div class="alert alert-danger mb-0">Attempts: {{ attempts }}</div>
    {% endif %}
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {# {% if app.user %} #}
        {# <div class="mb-3"> #}
        {# You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a> #}
        {# </div> #}
        {# {% endif %} #}

        <h1 class="h3 mb-3 font-weight-normal">Please insert card and enter PIN-code</h1>
        <label for="inputNumber">Number</label>
        <input type="text" name="number" id="inputNumber" class="form-control" required autofocus>
        <label for="inputPassword">PIN</label>
        <input type="password" name="password" id="inputPassword" class="form-control" required>

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
        #}

        <button class="btn btn-lg btn-primary" type="submit">
            Sign in
        </button>
    </form>
{% endblock %}
